node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
*.orig.*

# maplibre-gl's worker build, synced from node_modules by
# scripts/sync-maplibre-worker.js (runs via postinstall) — see the
# SDK-choice comment in src/components/MiniMap.web.tsx. Regenerated on
# every `npm install`/`npm ci`, so committing it would just go stale.
public/vendor/

# macOS
.DS_Store

# Xcode
ios/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate

# Android
android/
*.apk
*.aab

# Env
.env*.local
